package sorting;

public interface Sort {
    int[] sort(int[] arr);

    default void swap(int[] arr, int i, int j) {
        if (i == j) return;
        int temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;
    }
}
